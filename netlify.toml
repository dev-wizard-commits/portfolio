[build]
  command = "npm run build"
  # Note: Do NOT set a publish directory - the Next.js plugin handles this automatically
  # If you see an error about publish directory, remove it from Netlify UI: Site settings > Build & deploy > Build settings

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"

